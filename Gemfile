source "https://rubygems.org"

# Fastlane: iOS release automation (build, archive, upload to TestFlight).
# Pin to a known-good minor; `bundle update fastlane` to move forward intentionally.
gem "fastlane", "~> 2.225"
